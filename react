{movies.map(item=>{
return <li className={cureentIndex}>{item}</li>
movies.map((index,item)=>{
数组的map函数,curentIndex==index?'active':'--'}><{item}
点击做对饮过的切换
movies.map((item,index)=>{
return (
<li key={item.id} className={currenetIndex===index?'active':''}
onClick={()=>this.item.Click()}>

itemClick(inex_{
console.log('item Click',index
